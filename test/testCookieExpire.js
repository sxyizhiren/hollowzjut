
/*

测试cookie过期重启步骤：
手动在stateHand.js中
var countExpire=function(gsList){
    //gsList=[];//这一行注释去掉
    if(0 == gsList.length){
        cookieExpireTimes ++;
    }else{

    }
}
然后看能否退出并重启

    */